import SwiftUI

extension Color {
    /// Entu brand teal (#1E434C) — matches the webapp theme's `primaryColor`.
    static let entuBrand = Color(red: 0x1E / 255, green: 0x43 / 255, blue: 0x4C / 255)
}
